[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests
addopts = -m "not live"
markers =
    live: hits a running API; requires SMOKE_JWT and SMOKE_API
